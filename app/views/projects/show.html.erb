<p id="notice"><%= notice %></p>

<p><strong>Date:</strong>
  <%= @project.created_at%>
</p>

  <h1>
  <strong>Name:</strong>
  <%= @project.name%>
  <p>
    <strong>Skill Used:</strong>
    <%= @project.skill %>
  </p>
</h1>

<%#= image_tag @post.image.url(:medium) %>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>
<div>
  <br>

<h2> <%= @project.ratings.count %> ratings </h2>
  <%= render @project.ratings %>
   <h3> Add a rating: </h3>
 <%= render 'ratings/form' %>
 <br>
 </di>
  <% if current_user.admin? %>
<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'New', new_project_path(@project) %> |
<% end %>

<%= link_to 'Back', projects_path %>
